import React from "react";
import ReactDOM from "react-dom/client";

const App = () => {
  return <h1>Hello from Medtime Vite + React!</h1>;
};

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
